# Retrieval Policy

Start from Context, then Registry/index, then the smallest set of full documents, followed by code/tests/evidence when factual verification is required. Default full-document budget is small. Expand only for a named gap.

Prefer Git over projections. Feishu may be read for publication verification or native-source intake, but it cannot silently override Git.
