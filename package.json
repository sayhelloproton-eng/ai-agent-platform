{
  "name": "ai-agent-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A personal AI agent engineering platform for knowledge, execution, tools, and workflows.",
  "engines": {
    "node": ">=20 <21",
    "npm": ">=10 <11"
  },
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*",
    "packages/*",
    "capabilities/*"
  ],
  "scripts": {
    "check:repo": "node scripts/repo-check.mjs",
    "check:knowledge": "node skills/ai-knowledge/tests/self-test.mjs",
    "check:contracts": "npm run verify --workspace @ai-agent-platform/contracts",
    "check:auth": "npm run verify --workspace @ai-agent-platform/auth",
    "check:policy": "npm run verify --workspace @ai-agent-platform/policy",
    "check:gateway": "npm run verify --workspace @ai-agent-platform/action-gateway",
    "check:runtime": "npm run verify --workspace @ai-agent-platform/local-runtime",
    "check:edge": "npm run verify --workspace @ai-agent-platform/cloudflare-edge",
    "check:edge-bridge": "node --test scripts/edge-bridge-test.mjs",
    "check:local-chain": "npm run build --workspace @ai-agent-platform/local-runtime && npm run build --workspace @ai-agent-platform/action-gateway && node --test scripts/local-chain-test.mjs",
    "local:build": "npm run build --workspace @ai-agent-platform/contracts && npm run build --workspace @ai-agent-platform/auth && npm run build --workspace @ai-agent-platform/policy && npm run build --workspace @ai-agent-platform/local-runtime && npm run build --workspace @ai-agent-platform/action-gateway",
    "local:start": "npm run local:build && node scripts/local-stack.mjs",
    "edge:bridge:check": "node scripts/edge-bridge.mjs check",
    "edge:bridge:run": "node scripts/edge-bridge.mjs run",
    "check:local-stack": "npm run local:build && node --test scripts/local-stack-test.mjs",
    "verify": "npm run check:repo && npm run check:knowledge && npm run check:contracts && npm run check:auth && npm run check:policy && npm run check:gateway && npm run check:runtime && npm run check:edge && npm run check:edge-bridge && npm run check:local-chain && npm run check:local-stack"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.9.3"
  }
}
