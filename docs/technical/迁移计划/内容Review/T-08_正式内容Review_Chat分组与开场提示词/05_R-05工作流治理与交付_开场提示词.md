# R-05 工作流、治理与交付：Chat 开场提示词

请执行最终集中内容 Review 的 R-05，重点统一 Git 操作、确定性交付与 Git → Feishu 投影规则。

## 读取范围

- `docs/knowledge/07_工作流与项目治理/**`；
- `docs/technical/治理规则/**`；
- `docs/technical/技术方案/知识系统/**`；
- `skills/ai-knowledge/**`、`skills/deterministic-delivery/**`、`skills/planner-executor-handoff/**`；
- `platform-registry/**` 中直接相关的状态、关系与投影规则。

## 必查问题

1. 不再默认一个任务创建一个远程 Branch / PR；
2. Branch、Worktree、Push、PR、Merge 等动作全部由任务 Git Operating Policy 决定；
3. Git → Feishu 只能单向覆盖，不读取 Feishu 正文后合并回 Git；
4. 当前 Publisher 已支持 `asset:// → knowledge-assets → 上传 Feishu media → 插入 image block`；
5. 搜索并消除“飞书完全不接收图片”与当前 Publisher 实现的冲突；
6. 复杂图使用正式 SVG / PNG 资产，Mermaid 只用于简单局部关系；
7. 区分当前人工确定性交付和未来自动 Task Control；
8. Git 写入与 Feishu 发布必须分别授权。

## 输出

请返回治理冲突矩阵、旧规则命中位置、真实 Publisher 证据、建议保留/修正/归档项、逐文件建议和建议落库批次。当前 Review 不执行 Feishu 读写。
