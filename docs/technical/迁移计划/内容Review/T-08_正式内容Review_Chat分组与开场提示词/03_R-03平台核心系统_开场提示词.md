# R-03 平台核心系统：Chat 开场提示词

请执行最终集中内容 Review 的 R-03，以真实代码、测试和调用证据为最高优先级。禁止使用不存在的旧 Gateway / Runtime 包路径。

## 真实实现范围

- `apps/action-gateway/**`；
- `apps/local-runtime/**`；
- `apps/dev-tunnel/**`；
- `packages/contracts/**`；
- `packages/auth/**`；
- `packages/policy/**`；
- `scripts/local-chain-test.mjs`；
- `scripts/local-stack.mjs`；
- `scripts/local-stack-test.mjs`。

并核对对应的 `docs/knowledge/02_基础产品与能力/**`、`docs/knowledge/04_平台架构/**`、`docs/knowledge/08_实验与复盘/EXP-005-*`、`EXP-006-*` 及 `docs/technical/技术方案/**`。

## 已实现基线

Action Gateway、Local Runtime、Microsoft Dev Tunnels、Auth、Policy、Rate Limit、并发限制、Timeout、Task / Result / Error Contract、`gateway.ping`、`runtime.status`。

## 未实现基线

动态 Task Store、Execution / Result 持久化、Executor Adapter、Approval、Evidence、Side-effect Ledger、Health & Recovery、Execution Lane、多执行器调度与完整自动闭环。

## 输出

请返回逐项实现证据、文档声明与代码映射、夸大/过期/路径错误清单、逐文件建议和建议落库批次。不得以目标架构反推当前实现。
