# CTX-005 当前能力地图
> **资产状态**：本文正文已在 Batch 02 交付包中完成内容 Review；进入仓库后先标记为 `partial / unpublished`，待真实 Commit 整体复审通过后再升级为 `accepted`。

> 本文严格区分：已验证、部分实现、已设计、未开始、已替代和技术债务。审计基线为 `knowledge-rebuild-v2` 提交 `42306077c4d11773c510222abc7a79d83384bb36`；实现事实仍追溯到 `main` 的 `bd31893`，测试数字引用仓库验证结果。

## 1. 状态定义

| 状态 | 判定条件 |
| --- | --- |
| 已验证 | 有代码和实际测试、真实调用、回执或正式发布证据 |
| 部分实现 | 主边界存在，但范围窄、仍依赖静态配置或缺少完整治理 |
| 已设计 | 已形成经过讨论的模型和边界，但没有完整代码/测试 |
| 未开始 | 尚无对应实现，或只有占位说明 |
| 已替代 | 旧路线不再作为当前实现，但保留历史价值 |
| 技术债务 | 当前可以工作，但存在已知风险、漂移或待清理事项 |

## 2. 知识与上下文

### 已验证

- Git 是正式知识、代码、状态和规则的唯一真源；飞书为单向 Projection。
- `context/` 提供项目级最小启动上下文。
- `docs/knowledge/`、`docs/technical/`、`docs/learning/`、`docs/adr/` 四层边界已建立。
- AI Knowledge Skill v1.2.0 已用于受控投影、预览、人工确认和回读。
- 17 个知识页面曾发布到飞书知识库。
- 图片路线已形成 `knowledge-assets` + `asset://` + Publisher 上传方案。

### 部分实现与债务

- `docs/knowledge/` 仍是旧九栏目，尚未迁移到总控蓝图的 00～10 栏目。
- `platform-registry/` 已落地资产、关系、路径、迁移、Release 与 Feishu Projection 规则，并纳入根级校验。
- `architecture-context.md` 与当前 Runtime 实现存在 Drift。
- 飞书历史发布已完成，但当前 Phase 2 任务明确禁止未经授权继续写入或部署。

## 3. Contracts、Auth 与 Policy

### 已验证

- `@ai-agent-platform/contracts`：Task / Result / Error Contract v1、Capability 白名单、JSON 边界和运行时 validator。
- `@ai-agent-platform/auth`：Bearer 解析、API Key 格式校验、SHA-256 固定长度摘要、恒定时间比较和 Header 脱敏。
- `@ai-agent-platform/policy`：Deny by default、未知 Capability 拒绝、Allowlist 去重和固定顺序。
- Gateway 与 Runtime 使用分离的外部/内部 Key，并执行两层 Policy。

### 部分实现

当前认证是本机静态 API Key，适合 MVP，但没有自动轮换、用户身份、Agent Profile 动态映射、RBAC 或基于任务状态的细粒度授权。

## 4. Gateway 与 Local Runtime

### 已验证

- Gateway `/health`、`/ready`、安全 JSON、404/405、Request ID。
- 受保护的 `GET /v1/capabilities`、`POST /v1/tasks`、Custom GPT 专用零参数 `POST /v1/runtime/status`。
- Runtime `/health`、`/ready`、`/v1/tasks`、Body 限制、Task 校验、二次 Policy、Capability 调度和 `TaskResult`。
- Runtime Client 只允许 Loopback HTTP，具有默认 3000ms Timeout、响应大小限制和 Result 校验。
- 入站 Timeout、未读 Body 排空、Rate Limit、并发槽位和 Busy 映射已加固。
- 当前安全 Capability 为 `gateway.ping` 和 `runtime.status`；`system.info.safe` 未实现且默认拒绝。

### 边界

这是一条窄而可信的 Task 链路，不等于完整 Agent Runtime。它还没有通用 Task State、队列、重试编排、审批、补偿、持久事件日志和真实 Codex 执行适配。

## 5. Microsoft Dev Tunnels 与 Custom GPT Actions

### 已验证

- `apps/dev-tunnel` 使用官方 macOS x64 CLI，只公开 Gateway 8787；Gateway 和 Runtime 均只监听 Loopback。
- 本地与公网 health、未认证 401、已认证 capabilities 200、真实 `runtime.status` succeeded 已通过。
- 同一 Tunnel ID 重启 Host 后 URL 保持一致，30 天 refresh 已验证。
- OpenAPI 收窄为零参数 `getRuntimeStatus`；Builder、Preview 和正式 GPT 对话均已完成真实调用。

### 限制与债务

Dev Tunnels 为 Public Preview、无生产 SLA，连续 30 天无活动可能删除。它只用于开发期 MVP。Cloudflare 远端资源清理仍是独立债务，未经授权不得执行。

## 6. Skills 与 Agent 资产

### 已验证

- `skills/ai-knowledge/`
- `skills/custom-gpt-actions/`
- `skills/microsoft-dev-tunnels/`

这些 Skill 具有 README / SKILL / Schema / Example / Test 等工程边界。

### 已设计但未落地

- `agents/`：Agent Profile 配置真源；
- `knowledge-packs/`：通用基础 + 角色专属两层知识包；
- Agent 评估、发布、回滚和版本关系；
- 多 GPT 通过 Task State 协作，而不是共享会话记忆。

## 7. 测试与验证证据

仓库当前状态记录：

| 范围 | 结果 |
| --- | --- |
| Local Chain | 6/6 |
| Local Stack | 5/5 |
| Dev Tunnel | 44/44 |
| Runtime | 44/44 |
| Gateway | 79/79 |
| Policy | 12/12 |
| Contracts | 17/17 |
| Auth | 12/12 |
| Repo Check / Knowledge Skill / 根 verify | 通过 |

落盘前仍应在用户本机运行根级 `npm run verify` 和各 workspace 验证，避免状态文档与最新 Diff 再次漂移。

## 8. 目标设计与未开始能力

- 轻量 Task Control、状态机、版本检查、幂等命令；
- Approval、Evidence、Side-effect Ledger；
- Health Event、Checkpoint、Snapshot、Handoff、Recovery；
- Execution Lane、Branch / Worktree 与多执行器隔离；
- Platform Registry、关系、Projection 和变更影响；
- AI 视频工作流业务实现；
- 生产级公网入口和动态权限。

## 9. 已替代路线

- Git + Feishu 双源 / 双向同步；
- Feishu 合并反写 Git；
- GitHub Raw URL 直接作为飞书图片源；
- Cloudflare Edge 作为当前活跃入口；
- 当前阶段采用 LangGraph 作为核心 Runtime；
- 创建根级 `products/` 或提前创建大量空包。

## 10. 当前优先级

1. 修复 Context Drift，落位入口与产品文档；
2. 以当前安全链路为基础补 Task State、Evidence 和 Approval；
3. 形成 Agent Profile、Knowledge Pack 和 Registry MVP；
4. 再扩大 Capability 和 Codex 执行闭环；
5. 最后进入 AI 视频工作流业务验证。
