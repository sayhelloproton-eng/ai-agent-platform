# PRT-002 Demo 路线图

1. **Knowledge Demo**：知识治理、Registry、正式视觉资产和确定性投影已有仓库证据；最终 Feishu 发布与回读尚未执行。
2. **Coding Demo**：Handoff v0.4.0、冻结 Artifact、确定性交付、测试、Commit 与 Push 已有真实证据；动态 Task Store 和自动 Executor 调度尚未实现。Branch、Worktree 与 PR 只按任务 Git Operating Policy 使用。
3. **Runtime Demo**：Custom GPT → Dev Tunnels → Gateway → Runtime 的 `runtime.status` 窄链路已验证；完整 Agent Runtime、持久执行和生产公网入口尚未实现。
4. **Video Demo**：状态为计划；未来输入短故事，生成角色/场景/分镜/Prompt，调用可替换 Provider并记录成本、重试和评价。

每个 Demo 必须有可运行入口、失败案例、测试、架构说明和可量化结果。

正式 Portfolio Release 状态为 `planned / not_started`，需在 Feishu 发布、发布回读和最终整仓验收之后，汇总可运行 Demo、边界、代码与 Commit、测试、真实调用、失败修正、安全治理、Knowledge Projection、展示页、简历与面试材料、Release Tag 和敏感信息检查。
